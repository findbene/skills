# Extended Details

## Examples

**Example 1 — `/impeccable craft` for new landing**
- Input: "Build a premium landing page for a developer-tools startup"
- Action: Run context-gathering protocol → confirm vibe (technical, minimal, distinctive) → pick typography (Geist Mono headers + Geist body) → curated palette (mono with single accent) → asymmetric layout → magnetic CTA + scroll reveal → empty/loading states for product demo block
- Output: Distinctive landing, no generic-template smell, working code with motion

**Example 2 — `/impeccable extract` from an inspiration URL**
- Input: "Extract design tokens from https://example.com"
- Action: Follow extract flow → analyze typography, palette, spacing, motion → produce a design-tokens file the user can apply to their project
- Output: `design-tokens.css` + analysis report with rationale per token
